<nav class="main-nav" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to(root_path, class: 'navbar-brand') do %>
      <div>SHOUTER</div>
    <% end %>
  </div>

  <% if user_signed_in? %>
    <div class="navbar-search">
      <a href="#"><div class="icon search">Search</div></a>
      <input name="employee-search" type="text" placeholder="Search for an Employee">
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to new_shout_path, class: 'shout-link' do %>
            <div class="megaphone-wrapper"><div class="icon megaphone"></div></div><div class="header-text">Send a shout</div>
          <% end %>
        </li>
        <li>
          <%= link_to '#', class: 'profile-link dropdown-toggle' do %>
            <div class="profile-wrapper">              
              <% @user_avatar = current_user.avatar %>
              <% if @user_avatar %>
                <img src="<%= @user_avatar.url(:thumb) %>">
              <% end %>
            </div>
            <div class="header-text">
              <%= current_user.first_name %>&nbsp;<%= current_user.last_name %>
            </div>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "Edit Profile", edit_user_registration_path %>
              </li>
              <li>
                <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          <% end %>
        </li>
        <li class="nav-notification">
          <%= link_to '#', class: 'notification-link', data: { toggle: 'dropdown' } do %>
            <div class="icon bolt">Notifications<span class="notification-badge">12</span></div>
          <% end %>
        </li>
      </ul>
    </div>
  <% end %>
</nav>
